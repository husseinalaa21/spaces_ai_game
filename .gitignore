# Commit the generated Xcode project, shared scheme and Info.plist so
# Xcode Cloud can build directly after checking out this repository.
.DS_Store
DerivedData/
*.xcuserstate
xcuserdata/

# Local update archives and discarded drafts.
*.tar.gz
_to_delete/
